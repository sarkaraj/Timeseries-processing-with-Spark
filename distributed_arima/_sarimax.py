import pandas as pd
import numpy as np
import itertools
import warnings
import statsmodels.api as sm
from dateutil import parser
import distributed_arima.properties as p_model


def sarimax(prod, cus_no, mat_no, min_train_days=p_model.min_train_days, test_points=p_model.test_points):

    return None

